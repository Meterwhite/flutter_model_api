name: flutter_model_api
description: OOP for HTTP, network, async task
version: 1.0.0
homepage: https://github.com/hivedb/hive/tree/master/hive
documentation: https://github.com/hivedb/hive/tree/master/hive

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
